# 更新日志

## v1.4.1

### 修复

- **句首主语与游戏名粘连也能正确识别了**：此前只剔除「带空格 / 标点分隔」的前缀，「我r6玩了多久」这类**粘连说法**（`我` 紧贴 `r6`）无法识别，而单独的「r6」可以。现在对搜索词的扩展做了容错：会额外尝试「去掉句首主语/人称/请求前缀（我/我的/帮我/查…）后的变体」再去匹配，并让该变体继续走俗称映射。于是「我r6」→ 变体「r6」→ 正确命中《彩虹六号：围攻》；
- **安全前提不变**：原始关键词始终保留、只作为“再试一次”的额外候选，因此不会误伤真正以人称/动词开头的官方名（如「我的世界」= Minecraft、看门狗 = Watch Dogs）；
- **接口允许带上原始上下文**：LLM 辅助判游戏时不再只给被切过的关键词，而是带上尚未被切词的原始说法，让它对「不在预置俗称名单里的小众/口语称呼」判定更准。中招后仍未命中则仍回退到原有“近款建议”。

## v1.4.0

### 新功能

- **自然语言识别更聪明（可选 LLM 辅助）**：新增配置项 `nl_llm_assist`（默认开启）。当确定性关键词在用户真实游戏库里匹配不到时，会调用当前配置的 LLM 提供商一次，从该用户实际的游戏列表里判断最可能指的那款（如口语/俗称/别名变体），给出正确回答；匹配不到也绝不报错，自动回退到原有提示。若不想让插件调用 LLM，可在配置里关闭。
- **未 @ 也能点到群友**：在支持群成员名册的适配器上（如 aiocqhttp），说「看看 某某 的 奖杯/资料/游戏库/某某玩了多久」而没 @ 对方时，插件会先从群昵称识别到这位群友再查询；对方未绑定时给出友好提示，不再误查成自己。

### 修复

- **「我大镖客2玩了多久」不再把「我」当游戏名**：游戏名提取新增句首主语/人称/请求前缀剔除（我/我的/帮我/某人 等），并把句尾「我一共/我总共/白金了」等杂质一并清理，现在「@bot 我大镖客2玩了多久」能正确匹配到荒野大镖客2；
- **模糊搜索补全**：新增「r6 / 彩虹六号 / 彩虹六号围攻 / siege / 围攻 / 博德之门3 」等常用俗称的映射，解决部分简称/俗称命中不了的问题。

## v1.3.1

### 修复

- **自然语言「@某人 某游戏 玩了多久」不再误查自己**：此前发送「@bot @小明 艾尔登法环玩了多久」这类消息时，目标解析在 target 为空时直接回退到发起人自己的绑定，忽略了消息链里对「小明」的 @，导致查成了自己；同时游戏名关键词提取未清理 @ 片段，被 @ 的人名/QQ 号（如 `@小明(123456)` / `[At:123456]` / `@小明`）会混进游戏名参与模糊搜索。
  - 目标解析统一为「消息链中的真实 @ 提及 > 本人 > 文本参数」，自然语言路径（资料/游戏库/奖杯/指定游戏）与 LLM 工具兜底行为一致；
  - 新增 `_remove_at_fragments`：彻底移除文本中的 @ 提及片段（而非转成 QQ 号），自然语言与 `/psn游戏 @某人 游戏名` 指令路径共用，避免 QQ 号混入关键词后触发版本号校验误否决匹配；
  - 现在「@bot @小明 艾尔登法环玩了多久」会正确查询 **@小明** 的艾尔登法环游玩/奖杯情况。
- 顺带修复：`/psn游戏 @某人 <游戏名>` 指令路径中，`@昵称(qq号)` 被 `_strip_at_text` 转成裸 QQ 号残留进关键词，可能因版本号一致性校验（把 QQ 号当作版本号）导致找不到游戏的问题。

## v1.3.0

### 新功能

- **游戏名模糊搜索大幅增强**：查询指定游戏（`/psn游戏 <名称>` 与自然语言/LLM）不再要求名称精确一致。
  - 支持中文俗称 / 简称 / 英文正式名互通：如「老头环」「法环」「黑悟空」「黑神话悟空」「战神5」「诸神黄昏」「美末2」「大镖客2」「怪猎荒野」「ff7重生」「赛博朋克」等都能正确命中；
  - 内置常见游戏的中英文别名表（艾尔登法环、战神、最后生还者、蜘蛛侠、地平线、怪物猎人、最终幻想、生化危机、GTA、赛博朋克、黑神话·悟空等）；
  - 自动归一化：忽略全角/半角、大小写、音标（Ragnarök）、版本/平台后缀（Remaster、Remake、Director's Cut、PS4/PS5、Edition 等）、空格与标点；
  - 分层打分：精确 > 前缀 > 包含 > 内容词高相似 > 中文子串，并按游玩时长决定同分时的先后；
  - **版本号一致性校验**：带版本号的查询（如「美末2」「黑魂3」「战神5」）不会误匹配到前作；用副标题区分的作品（如 God of War Ragnarök）也能正确识别；
  - 找不到时会给出库中最接近的几个游戏名作为「你是不是想找」提示。
- **进度提示自动撤回**：资料/游戏库/奖杯/指定游戏/排行/对比/联动/在线/绑定等较慢查询，会先通过 OneBot 底层接口发送「正在获取…」提示，结果图片发出后自动撤回该提示，让对话更干净（参考 astrbot_plugin_help_typst 的做法）。非 OneBot 平台自动降级为普通提示（不撤回）。

### 修复

- 自然语言/LLM 查询时的「正在…」进度消息不再依赖 pipeline 的 yield 时序，避免在个别时序下提示或结果丢失。

## v1.2.2

-  **修复自然语言查询只回复「正在获取…」、不返回结果图片（且无报错）**：根因是自然语言分发器在**发出进度提示之前**就调用了 `event.stop_event()`。AstrBot 的管线是洋葱模型——一旦事件被标记停止，调度器在「本次 yield 的后续阶段跑完」后即 `break`，生成器不再恢复，因此进度消息能发出，而随后的 `yield 图片` 根本没机会执行（所以无报错但图片丢失；`/psn` 等指令因不调用 `stop_event()` 而正常）。现统一为正确顺序：先发进度提示（不 stop）→ 等待 PSN 返回 → 最后 `stop_event()` 并 yield 一次最终结果，图片正常返回。

## v1.2.1

-  **修复自然语言查询时报「处理 PSN 请求时出现错误：cannot unpack non-iterable coroutine object」**：v1.2.0 的自然语言分发器（含 `psn_query_game` 工具）在调用异步的 `_tool_resolve_target()` 解析查询目标时漏写了 `await`，拿到的是协程对象而非 `(online_id, error)`，导致解包失败。现补齐 3 处缺失的 `await`（个人资料/游戏库/奖杯、指定游戏、LLM 游戏工具）。指令路径 `/psn` 等因走同步目标解析不受影响，故仅自然语言触发报错。

## v1.2.0

-  **新增「指定游戏详情」查询**：可用 `/psn游戏 <游戏名关键词>`（或自然语言「艾尔登法环玩了多久」「黑神话悟空游戏信息」）查看某一款游戏的总游玩时长/小时、启动次数、首次与最近游玩日期，以及该游戏的奖杯完成度与白金/金/银/铜进度（新增 `game.html` 模板）。游戏名支持模糊匹配（精确 > 前缀 > 包含）。
-  **自然语言识别改为插件层确定性处理，彻底绕过其他插件干扰**：
  - 此前 LLM 触发「psn奖杯」等请求时，若同时启用了 `memory_companion` / `private_companion` 等接管 Agent 的插件，它们可能在 LLM 选择工具前就拦截请求（用记忆里的历史失败话术直接回复，或用私有 Agent 屏蔽本插件工具），导致工具根本不被调用、机器人回「PSN 接口有问题」。
  - 现新增基于 `@filter.regex` 的意图分发器，运行在**插件处理阶段（早于 Agent/LLM 阶段）**：命中 PSN 意图后直接查询并 `stop_event()`，事件不再流入任何 LLM/Agent 插件，因此不受上述插件影响，也**不强制要求配置 LLM**。
  - 原有的 8 个 `@filter.llm_tool`（新增 `psn_query_game`）保留作为意图模糊时的兜底。
  - 显式指令（`/psn...`）仍走原命令路径，不受影响；意图识别覆盖资料/游戏库/奖杯/单款游戏/排行/对比/联动/在线/绑定/解绑/同步/帮助。
- 内部：抽取 `_do_network`（群联动）为共享方法；自然语言分发器与命令、LLM 工具复用同一套 `_do_*` 业务逻辑。

## v1.1.5

- 修复自然语言查询时模型不调用工具、直接回复「接口不稳定/查不了」：之前版本的工具异常会进入对话历史，模型据此误判工具长期不可用而跳过调用。现强化所有 LLM 工具的描述，明确要求每次都必须实际调用、不得凭历史对话宣称失败；配合 v1.1.4 的异常兜底，工具结果会以真实消息返回。**升级后请重载插件并清空/新建会话**，以丢弃历史失败上下文。

## v1.1.4

- 修复自然语言（LLM 工具）触发时回复「PSN 接口不稳定 / 暂时查不了」：工具运行在 Agent 的工具执行器中，任何未捕获的异常都会被吞掉、不返回真实原因。现给所有 LLM 工具（资料/游戏库/奖杯/排行/对比/在线/绑定）加上统一异常兜底，出错时把完整堆栈写入日志，并向用户返回具体可读原因，而不是被 Agent 笼统转述为「接口不稳定」。

## v1.1.3

- 修复 LLM 工具触发时报错「工具接口有点问题，暂时查不了」：v1.1.1 引入的统一工具出口 `_yield_tool_result` 误写为同步生成器却被 `async for` 迭代，导致 `TypeError: 'async for' requires ... got generator`。改为异步生成器，所有 LLM 工具（资料/游戏库/奖杯/排行/对比/在线/绑定）恢复正常。

## v1.1.2

- 文档/配置：NPSSO 获取指引中不可用的登录地址 `my.account.sony.com` 改为 PlayStation 中国官网 `https://www.playstation.com/zh-hans-cn/`，同步更新配置项提示与 README。

## v1.1.1

- **修复 LLM 触发时机器人重复回复两条相同消息**：本地函数工具产出结果后，核心会立即直接发送，同时 RespondStage 又会把同一结果再发一遍。现统一走工具出口，登记已发送文本做去重，确保只发一次。
- **修复 LLM 查询已绑定用户却提示「未找到该 PSN 用户」**：当 LLM 把用户自己的群昵称/「我」等当作 target 传入时，会误当成 PSN 在线 ID 提交。现识别「我/自己/我的/本人昵称/本人QQ号」等自称，自动回退到发起人自己的绑定。
- **自然语言绑定直接生效**：把原 `psn_bind_help`（仅提示用户手敲指令）改为 `psn_bind`，说「绑定psn，ID是 xxx」会直接校验并完成绑定，无需再手动发送 `/绑定psn`。

## v1.1.0

- **自然语言（LLM）支持**：新增 7 个函数调用工具（`@filter.llm_tool`），不必记忆指令，直接 @机器人 说「查下我的奖杯」「群里谁最肝」「看看 @某人 在玩什么」「和 @某人 对比一下」等自然语言即可触发。需在 AstrBot 中配置并启用 LLM。
- **修复 `/psn @xx` 报错**：
  - 正确解析消息链中的 `At` 组件，并排除 @机器人 自身（群聊唤醒时常带）；
  - 当被 @ 的人未绑定 PSN 时，返回清晰的「该用户尚未绑定」提示，而不是把 `@昵称(QQ号)` 脏字符串当作 online_id 提交给 PSN，导致「未找到 PS online ID」错误；
  - 同时清理文本参数中残留的 `@昵称(qq号)` / `[At:qq号]` 占位符，兼容不同适配器。
- 内部重构：将个人资料 / 游戏库 / 奖杯 / 排行 / 对比 / 在线等核心查询逻辑抽取为可复用方法，供指令与 LLM 工具共用，行为一致。

## v1.0.2

- 调整：`/psn帮助` 强制以纯文字发送（`use_t2i(False)`），避免长帮助文本被转成图片，方便复制指令。

## v1.0.1

- 修复：`html_render` 误传模板文件路径导致返回白图（仅显示路径文字）的问题，改为传入模板内容；
- 调整：`/psn启用`、`/psn禁用` 使用 `PermissionType.ADMIN` 装饰器，默认仅管理员可调用。

## v1.0.0

首个版本：

- 白名单群聊启用（配置 + `/psn启用`、`/psn禁用` 管理指令）；
- `/绑定psn`、`/解绑psn`、`/psn同步` 账号管理；
- `/psn` 个人资料、在线状态、正在游玩、奖杯总览；
- `/psn游戏库` 游戏库、游戏时长、平台统计、封面墙；
- `/psn奖杯` 每款游戏的奖杯完成度与进度条；
- `/psn排行` 支持时长 / 游戏数 / 奖杯总分 / 白金杯排行；
- `/psn对比` 双人数据对比与共同游戏；
- `/psn联动` 群内 PSN 好友关系与共同游戏分析；
- `/psn在线` 群内在线状态与正在游玩；
- 基于 AstrBot HTML 渲染的深色风格可视化图片；
- 内存缓存与本地使用日志。
